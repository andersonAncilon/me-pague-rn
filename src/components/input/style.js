import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    myInput: {
        backgroundColor: 'rgba(251, 252, 252, 0.7)',
        borderRadius: 8,
        width: '90%',
    },

    myLabel: {
        color: 'black'
    }
});